# TODO: Create API for Demand-Notes

## Completed Tasks
- [x] Created `/api/demand-notes/route.ts` with GET and POST endpoints
- [x] Ran `npx prisma generate` to update Prisma client types

## Pending Tasks
- [ ] Update DemandNoteForm component to submit data to POST API
- [ ] Update demand-notes page to fetch data from GET API instead of mock data
- [ ] Test the API endpoints
- [ ] Handle file uploads in the API if needed
